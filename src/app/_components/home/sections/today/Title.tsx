const Title = ({ title }: { title: string }) => {
    return (
        <p className="text-xl lg:text-2xl text-white font-sfh uppercas">
            {title}
        </p>
    );
};

export default Title;
