export default function Header() {
    return (
        <header className="sticky top-0 z-10 w-full h-12 flex items-center justify-between px-4 backdrop-blur-xl">
            <p className="text-xl font-sfb tracking-wide">Categories</p>
        </header>
    );
}
